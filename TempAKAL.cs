using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TempAKAL : MonoBehaviour
{
    /*
    Camera mainCam;
    AkAudioListener akAudioListener;
    // Start is called before the first frame update
    void Start()
    {
        akAudioListener = mainCam.GetComponent<AkAudioListener>();
        akAudioListener.
  
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
